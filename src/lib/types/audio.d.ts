/**
 * Audio type declarations for Do it for the XP
 */

interface Window {
  /**
   * Play a sound effect
   * @param effectName Name of the effect to play
   */
  $play$fx: (effectName: string) => void;
}